# Decisions Log

- **S1 — Foundation:** Next.js pinned to 15.x per §2 (create-next-app now defaults to 16). shadcn-style UI primitives hand-rolled on Radix (popover) and themed to §15 tokens; identical API, no extra component library. `.env.example` added.
- **S2 — Database:** Migrations 0001–0008 cover the full blueprint §38 schema. `question_bank`, `personas`, `rubric_prompts`, and `usage_log` have **no user read policy** — answers/prompts are served only by server code via the service client. Types generated from a live Postgres into `src/types/database.ts` (Supabase CLI gen-types needs Docker, unavailable in the build environment; the script output matches the CLI format). Seed uses fixed UUIDs so deep links are deterministic. 7 "In Training" stubs seeded per §5 (Secular Skeptic deferred — §5 says 7 stubs); stub one-liners live in code since `tracks` has no description column (schema is locked).
- **S3 — Auth + shell:** Profile + name created by a DB trigger on signup (`handle_new_user`). Middleware guards all app routes; logged-in users are bounced from `/`, `/login`, `/signup` to the dashboard.
- **S4 — Dashboard + tracks:** "Lesson read" = `xp_events` row with reason `lesson_complete`; "lesson complete" = a passed checkpoint attempt — no separate lesson_progress table needed for v1.
- **S5 — Lesson reader:** Lesson copy ships verbatim; glossary callouts are listed in `content.terms` (rendered as tap-open chips) so the approved prose is untouched. Added `POST /api/learn/[lessonId]/complete` (not in §4's list but required by the LessonCompleteCTA — simplest implementation). Verses render client-side through `/api/verses` so the ESV cache write-through is exercised on every view.
- **S6 — Quiz:** Grading fully server-side; the response reveals correctness + teaching note + source-block deep link but never the correct option. Pass threshold read from `quizzes.config` (0.7). XP on first pass only.
- **S7 — Flashcards:** Interval snapping interpreted as nearest-of-{1,3,7,14,30,90} until raw > 90 (matches the acceptance sequence 1 → 3 → 7). Scripture mode ladder advances when the new interval reaches ≥7; `again` regresses one mode step. Queue-completion XP capped at once per day.
- **S8 — Respond:** Drill objections are the lesson's steelmanned pushbacks, kept in code (`lib/ai/prompts/respond.ts`) and rotated daily — no schema change. Scoring via Sonnet (`claude-sonnet-4-6`) with Zod-validated output; every call logged to `usage_log`.
- **S9 — Simulator:** Persona turns via Haiku (`claude-haiku-4-5`), temperature 0.8, streamed as plain text (AI SDK `streamText` server-side; a lightweight reader client-side instead of `useChat` — fewer moving parts, same streamed UX). Messages persisted server-side on each turn; refresh restores the thread from the DB.
- **S10 — Coach:** Citation-or-silence enforced in code: invalid `citation_source_id` → one retry → nulled + `flagged=true`. Quoted moments are checked against the transcript; misses flag the review for audit. Simulation XP scales 50–250 on the rubric average. Focus mode (`data-mode="focus"`) wraps /review, /respond, and all /spar pages.
- **v0.2 — Feedback round 1 (Melo):** Honest scoring: rubric prompt v2 (DB) + scoring-discipline rules in both AI prompts; a non-answer scores 1s, and respond/spar XP now scales from 0 (avg 1 → 0 XP). Profile icon opens a menu (account/how-it-works/sign out) instead of signing out; new /account page with stats, name editing, and plain-English explanations of XP/streak/levels. Review session got an instructions screen and the grade buttons got plain-language hints. Inline scripture references in prose are auto-detected and open verse popovers (/api/verses made public + CDN-cached for the marketing pages). Glossary terms got full multi-sentence definitions (DB). Trinity lesson deepened from 16 to 24 blocks (echad + homoousios word studies, wider deity case, Nicaea history, harder follow-ups, why-it-matters) and the checkpoint went from 8 to 24 questions — all in update-app-v02.sql since content lives in the DB. New public site: /about (philosophy + What Contend Affirms), /training catalog with a preview article per subject (copy in src/lib/site-content.ts), /how-it-works (method, scoring, XP/streak/levels), shared site header/footer, richer landing. Course model reframed: finish any course to unlock the next choice, completed courses stay open (display logic; gate enforcement still a later milestone). Design pass: scripture/word-study set pieces, drop caps, dashboard 4-card layout, paper grain, hover lifts. Google button now surfaces provider-not-enabled errors; enabling Google OAuth requires Supabase + Google Cloud setup (user-side).
- **v0.3 — Feedback round 2 (Melo):** Logo: shield-and-cross-sword SVG mark (components/layout/logo.tsx) used across site/app headers, footer, and favicon (app/icon.svg). Motion system in globals.css (scroll reveals via IntersectionObserver wrapper, animated link underlines, card lift/press, shake/pop keyframes, all honoring prefers-reduced-motion); buttons got press-scale and layered shadows. Landing redesigned with dark ink hero (oxblood/gold radial glows, giant watermark mark, staggered reveals). Games arena at /games (signed-in only, focus mode): The Gauntlet (server-graded speed quiz over the checkpoint bank — answers never reach the client, 3 lives, shrinking clock, streak multiplier), Term Match (timed glossary pair-matching, 3 levels), Creed Builder (assemble the six-line formulation in order; stage 2 hides three named heresies as fatal distractors). Game XP: 15 per game per day via /api/games/complete (reason game_<name>), enforced server-side.
- **v0.4 — Course progression + Roman Catholicism + assessment (Melo round 3, part 1):** Course graph in src/lib/courses.ts — 10 numbered courses (Foundations 1–5, Engagements 6–10) with a prerequisite map; a course unlocks only when every prereq course's checkpoint is passed (in-production prereqs therefore gate downstream courses, by design). Trinity renumbered to 03; added Roman Catholicism as 06 (engagement) requiring Trinity. /tracks rewritten with numbered, prereq-gated cards; /tracks/[slug] enforces the gate on direct nav and shows per-track bibliography; /training catalog ordered by course number. New Roman Catholicism course (update-contend-v04.sql): 19 lesson blocks (sola scriptura, sola fide/justification imputed-vs-infused, Trent's anathemas, the Mass/transubstantiation vs Hebrews 10, purgatory/indulgences/1517, papacy, the four Marian dogmas, steelmanned objections, response formulas, gospel), 8 glossary terms, 4 sources (Luther, Trent, Catechism, Melo), 14-question checkpoint (quiz id …002), an 8-card deck. Placement assessment (#5): 14 server-graded questions across 5 domains in src/lib/assessment.ts (answers server-only, sanitized over /api/assessment), tiers (New Recruit→Veteran), per-domain readout, and a recommended starting course; stored in intake_attempts; dashboard banner prompts it until taken. Lesson header eyebrow now derives the track title/level dynamically.
- **v0.5 — Social: friends, leaderboard, study groups + chat (Melo round 3, part 2):** Migration 0009_social.sql (= update-contend-v05-social.sql) adds friendships, game_scores, study_groups, group_members, group_messages, an is_group_member() security-definer helper, and broadens profiles SELECT to authenticated (display-name visibility for leaderboards/friends). Friends: request/accept/remove via /api/friends (+ name search); /friends manager UI. Leaderboard: best Gauntlet score per user recorded via /api/games/score; /leaderboard shows a friends circle + global top 50. Study groups: create/join-by-code/leave via /api/groups; /groups list and /groups/[id] detail (invite code, members with per-course completion computed via service client after membership check, group discussion thread). Per-lesson group discussion embedded at the end of each lesson (LessonGroupDiscussion → group_messages scoped to group+lesson). All group writes go through server routes (service client); reads are membership-gated by RLS. database.ts hand-extended with the 5 new tables. Nav: Groups added to header; Leaderboard + Friends in the account menu and Games hub.
- **v0.6 — RC 404 fix + resources, listen-aloud, mobile menu, notes (Melo round 4):** Root cause of the Roman Catholicism 404 was that update-contend-v04.sql had not been applied (the course is data-driven; no track row → notFound). Shipped one self-contained, idempotent `witness-ready-catholicism-course.sql` (full course, upserts) so there's no multi-file ambiguity; /tracks/[slug] now shows a "content not loaded" panel for a known-but-missing course instead of a bare 404. Built the course from Melo's 10 evangelical works (Allison, White, Chemnitz, Webster, McCarthy, Mathison, Ortlund, Geisler/MacKenzie, Zins, Wells) added as sources + citations (all 14 sources now appear in the course bibliography), plus two new lesson blocks the resources fuel: "Rome at the bar of history" (continuity/development) and "Did Vatican II change Rome?" — and a 15th checkpoint question. Listen-aloud: ReadAloud client component using the browser SpeechSynthesis API (no API cost), sticky control on lessons, play/pause/resume/stop + speed; text assembled server-side via lib/lesson-tts.ts (markdown stripped, model_answer skipped). Mobile nav: app header now has a hamburger (sm:hidden) opening a slide-down menu with all sections; desktop unchanged. Notes/journal: migration 0010_notes.sql (notes table, RLS own-only) + database.ts type; /notes page with NotesManager (search, create, edit, delete, grouped by course via courses.ts); NoteEditor with bold/italic/bullet toolbar + live preview; notes stored as a small markdown subset rendered by lib/notes-format.tsx; LessonNotes embedded at the end of each lesson auto-tags course + lesson + chosen section, so notes self-organize. Notes link added to the account menu and mobile menu; /notes added to protected routes.

- Rich notes editor (WYSIWYG bold/italic/underline/bullets, stored as sanitized HTML; legacy markdown notes still render). New listen-aloud player: studio TTS via OpenAI (cached in Supabase Storage "lesson-audio" bucket) with male/female voices, draggable scrubber, exact duration/remaining, ±10s, speed — falls back to improved browser voices when OPENAI_API_KEY is absent. Logo is now a wordmark ("Witness" ink + "Ready" oxblood); shield retired; favicon is a WR monogram. Deepened oxblood accent (#8a2329). Landing and Games pages reworked into editorial/arena layouts to break the templated look.

- Full design-system overhaul ("Contend" direction): dark-first theme (#0B0B0F canvas, #ECECF1 text), electric cobalt accent (#4D6BFF) replacing oxblood, warm gold secondary. New type: Space Grotesk (display) + Inter (body), replacing Fraunces. Real logo: constructed monoline WR monogram in a cobalt gradient badge + wordmark lockup; favicon matches. Tokens drive the whole app dark; audited and fixed raw light-mode tokens (text-ink→text-foreground, ink-soft repurposed to steel, oxblood glows→cobalt). Buttons/cards get cobalt glow + lift. Landing, games, dashboard, header/footer, auth pages restyled.

- Re-themed to "Divinity" scholarly/seminary direction: light-first warm bone paper (#F4F0E6), espresso ink, deep evergreen accent (#1E4D3A) + brass (#B08D3E); cobalt/dark retired. Type changed to Newsreader (serif display, incl. italic scripture) + Inter body; lesson prose now set in the serif for a book feel. Logo rebuilt as an academic seal: brass double-ring crest topped by a cross with a serif WR; favicon matches. Sparring/games stay dark (focus mode). Landing glows recolored to evergreen/brass.

- Business Insider editorial layout language (kept the Divinity palette/serif): a "theological broadsheet" pass. New reusable primitives (Kicker, Meta, SectionHeading, Rank) in components/ui/editorial.tsx. App and public headers rebuilt as mastheads with a newsroom section/category bar. Dashboard rebuilt as a front page (dateline, lead story, on-the-desk rail). Courses page rebuilt as a ranked, hairline-divided news index. Lesson reader given an article header (kicker + headline + byline/meta rule). Landing rebuilt as a light front-page masthead.

- Palette switched to white / blue / gold (collegiate): white background, near-black ink, royal-blue accent (#1D4ED8), gold secondary (#B5893C); evergreen retired. Readability pass: softened the heavy 2px black rules to light hairlines, more whitespace. Renamed jargon ("On the desk" -> "Up next", "Honors" -> "Your badges") and plainer dashboard copy. Dashboard "Up next" is now stacked cards (no internal dividers). Courses rebuilt as a clean scannable card grid with status pills. Games page reworked to match the site (light, blue/gold cards) instead of the dark "arena" look. Focus mode (review/spar/respond) is now a calm deep navy rather than near-black. Logo seal + favicon recolored (gold ring, blue WR).

- Added the theological foundation: a data-driven "What We Believe" page at /beliefs (lead confession, 15 affirmations, doctrine sections on Scripture/Trinity/Christ/Spirit/Gospel, "guarding the lines", why-the-creeds, doctrinal standards). Content typed in src/lib/beliefs.ts; full 15-deliverable source (incl. heresy-avoidance author guard, training-shaping notes, build structure, UI vs robust copy) in docs/beliefs.md. Linked from footer, public nav, signed-in menu, and a short homepage confession band.
- Extended the white/blue/gold design system to the remaining pages (about, how-it-works, training + previews, account, notes): standardized text-muted-fg, bg-surface cards, shared Kicker/SectionHeading/Button primitives, card-interactive hovers. Game-play and remaining notes components already token-clean.
